{
  "name": "groc-ecommerce",
  "version": "1.0.0",
  "description": "GROC MERN Stack E-Commerce",
  "main": "backend/server.js",
  "scripts": {
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
